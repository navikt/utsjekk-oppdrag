spring:
  cloud:
    vault:
      enabled: true
      uri: https://vault.adeo.no
      connection-timeout: 5000
      read-timeout: 15000
      config:
        order: -10
        lifecycle:
          enabled: true
          expiry-threshold: 2m
      database:
        enabled: true
        role: dp-oppdrag-admin
        backend: postgresql/prod-fss
      authentication: KUBERNETES
      kubernetes:
        role: dp-oppdrag
        kubernetes-path: kubernetes/prod/fss
        service-account-token-file: /var/run/secrets/kubernetes.io/serviceaccount/token
      generic:
        enabled: true
        backend: kv/prod/fss
        application-name: dp-oppdrag/default
        default-context: dp-oppdrag/default
